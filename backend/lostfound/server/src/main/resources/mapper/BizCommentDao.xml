<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.qg.server.mapper.BizCommentDao">
    <resultMap id="CommentStatVO" type="com.qg.pojo.vo.CommentStatVO">
        <id property="id" column="id"/>
        <result property="itemId" column="item_id"/>
        <result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/>
        <result property="content" column="content"/>
        <result property="createTime" column="create_time"/>
        <result property="isRead" column="is_read"/>
    </resultMap>

    <select id="selectCommentList" resultMap="CommentStatVO">
        SELECT
            c.id, c.item_id AS itemId, u.nickname, u.avatar, c.content, c.parent_id AS parentId, c.create_time AS createTime
        FROM
            biz_comment c
                LEFT JOIN sys_user u ON c.user_id = u.id
        WHERE
            c.item_id = #{itemId}
          AND c.deleted = 0
        ORDER BY
            c.create_time DESC
    </select>


</mapper>